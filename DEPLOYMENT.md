# Руководство по развертыванию Football-Stat

В этом документе описаны все шаги для настройки, разработки и деплоя проекта Football-Stat.

## 📋 Содержание

1. [Локальная разработка](#локальная-разработка)
2. [Настройка Git-репозитория](#настройка-git-репозитория)
3. [Настройка NAS для деплоя](#настройка-nas-для-деплоя)
4. [Настройка CI/CD с GitHub Actions](#настройка-cicd-с-github-actions)
5. [Диагностика проблем](#диагностика-проблем)

## 🖥️ Локальная разработка

### Предварительные требования

- Docker и Docker Compose (для контейнеризированного и гибридного подходов)
- Node.js и npm (для локального фронтенда)
- Python 3.10 или выше (для локального бэкенда)
- PostgreSQL (для локального бэкенда)
- Git
- Bash (для Linux/macOS) или Git Bash/WSL (для Windows)

### Варианты настройки

Проект поддерживает три разных подхода к настройке локальной среды разработки:

1. **Полная контейнеризация** - все компоненты (бэкенд, фронтенд, БД) запускаются в Docker-контейнерах
2. **Гибридный подход** - бэкенд и БД запускаются в Docker, фронтенд - локально
3. **Полностью локальная настройка** - все компоненты запускаются на локальной машине без Docker

Для выбора подходящего варианта используйте мастер-скрипт настройки:

```bash
./setup-all.sh
```

#### 1. Полная контейнеризация

Преимущества:
- Изолированная среда
- Единая конфигурация для всех разработчиков
- Быстрый старт без установки дополнительных зависимостей

```bash
# Настройка и запуск
./setup-project.sh

# Управление контейнерами
docker-compose up -d        # Запуск всех сервисов
docker-compose down         # Остановка всех сервисов
docker-compose logs -f      # Просмотр логов
```

#### 2. Гибридный подход

Преимущества:
- Быстрая разработка фронтенда с автоматической перезагрузкой
- Не требуется устанавливать БД локально
- Бэкенд и БД изолированы в контейнерах

```bash
# Настройка и запуск бэкенда и БД
docker-compose up -d db redis api

# Настройка и запуск фронтенда локально
./setup-frontend-local.sh

# Последующие запуски фронтенда
cd frontend && npm run dev
```

#### 3. Полностью локальная настройка

Преимущества:
- Быстрая разработка без накладных расходов на контейнеризацию
- Более высокая производительность
- Упрощенная отладка

```bash
# Полная настройка локальной среды
./setup-local.sh

# Последующие запуски
./start-backend.sh          # Запуск бэкенда
./start-frontend.sh         # Запуск фронтенда
```

### Доступные сервисы

После запуска, независимо от выбранного подхода, доступны следующие сервисы:
- **API**: http://localhost:8088
- **API Документация**: http://localhost:8088/docs
- **Frontend**: http://localhost:3000

## 🔄 Настройка Git-репозитория

Для настройки Git-репозитория и подключения к GitHub:

1. Запустите скрипт инициализации репозитория:
   ```bash
   ./init-repo.sh
   ```
   
   Этот скрипт выполняет следующие действия:
   - Инициализирует Git-репозиторий (если он еще не инициализирован)
   - Настраивает удаленный репозиторий GitHub
   - Создает коммит с изменениями
   - Отправляет изменения на GitHub (опционально)
   - Настраивает базовый GitHub Actions workflow (опционально)

2. После настройки репозитория вы можете отправить изменения на GitHub:
   ```bash
   git push -u origin main
   ```

## 🖧 Настройка NAS для деплоя

Для настройки Synology NAS в качестве сервера для деплоя:

1. Запустите скрипт настройки NAS:
   ```bash
   ./setup-nas.sh
   ```
   
   Этот скрипт выполняет следующие действия:
   - Настраивает SSH-подключение к NAS
   - Создает SSH-ключи (если они отсутствуют)
   - Добавляет публичный ключ на NAS
   - Проверяет наличие Docker и Docker Compose на NAS
   - Создает директорию проекта на NAS
   - Генерирует конфигурацию .env.production и копирует её на NAS

2. Требования к NAS:
   - Synology DSM 7.0 или выше
   - Установленный Docker
   - SSH-доступ с правами администратора

## ⚙️ Настройка CI/CD с GitHub Actions

Для настройки автоматического деплоя на NAS при каждом пуше в ветку main:

1. Запустите скрипт настройки GitHub Secrets:
   ```bash
   /Volumes/docker/scripts/setup-github-secrets.sh
   ```
   
   Этот скрипт поможет создать и настроить необходимые секреты для GitHub Actions:
   - NAS_HOST - IP-адрес вашего NAS
   - NAS_USERNAME - имя пользователя SSH для NAS
   - NAS_SSH_PRIVATE_KEY - приватный SSH-ключ для доступа к NAS
   - NAS_KNOWN_HOSTS - fingerprint SSH вашего NAS
   - PROD_ENV_FILE - содержимое файла .env.production

2. После настройки секретов, GitHub Actions будет автоматически деплоить проект на NAS при каждом пуше в ветку main.

## 🔍 Диагностика проблем

### Проблемы с Docker

Если у вас возникли проблемы с Docker:

```bash
# Проверка статуса контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Перезапуск контейнеров
docker-compose restart
```

### Проблемы с API

Для диагностики проблем с API:

```bash
# Проверка доступности API
curl http://localhost:8088/health

# Запуск диагностического скрипта
python backend_diagnostic.py
```

### Проблемы с деплоем на NAS

Если возникли проблемы с деплоем на NAS:

1. Проверьте SSH-подключение:
   ```bash
   ssh <NAS_USERNAME>@<NAS_HOST>
   ```

2. Проверьте наличие директории проекта на NAS:
   ```bash
   ssh <NAS_USERNAME>@<NAS_HOST> "ls -la /volume1/docker/football-stat"
   ```

3. Проверьте логи GitHub Actions в разделе Actions вашего репозитория.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в GitHub-репозитории проекта или свяжитесь с разработчиками напрямую. 